# Implementation Plan - Phase 4: Monsters and AI Modularization

- [ ] 1. Monster Dedicated Headers Creation
  - [ ] 1.1 Create headers for human & humanoid characters (`barney.h`, `scientist.h`, `hgrunt.h`, `hassassin.h`, `gman.h`, `genericmonster.h`, `monster_deadhev.h`)
    - Extract class declarations, secondary entities (`CDeadBarney`, `CDeadScientist`, `CSittingScientist`, `CHGruntRepel`, `CDeadHGrunt`), schedule enums, task enums, and save data prototypes.
    - _Requirements: REQ-4.1_
  - [ ] 1.2 Create headers for alien wildlife & xen creatures (`agrunt.h`, `apache.h`, `barnacle.h`, `bigmomma.h`, `bloater.h`, `bullsquid.h`, `controller.h`, `gargantua.h`, `headcrab.h`, `houndeye.h`, `ichthyosaur.h`, `islave.h`, `leech.h`, `nihilanth.h`, `osprey.h`, `rat.h`, `roach.h`, `tentacle.h`, `zombie.h`, `aflock.h`)
    - Extract class declarations, flocking entities (`CFlockingFlyer`, `CFlockingFlyerFlock`), schedule enums, task enums, and save data prototypes.
    - _Requirements: REQ-4.1_

- [ ] 2. Monster Sub-Entities and Projectiles Extraction
  - [ ] 2.1 Extract Apache HVR rocket (`apache_hvr.h`, `apache_hvr.cpp`)
    - Move `CApacheHVR` out of `apache.cpp`.
    - _Requirements: REQ-4.2_
  - [ ] 2.2 Extract Bullsquid spit projectile (`bullsquid_spit.h`, `bullsquid_spit.cpp`)
    - Move `CSquidSpit` out of `bullsquid.cpp`.
    - _Requirements: REQ-4.2_
  - [ ] 2.3 Extract Baby Headcrab entity (`baby_headcrab.h`, `baby_headcrab.cpp`)
    - Move `CBabyCrab` out of `headcrab.cpp`.
    - _Requirements: REQ-4.2_
  - [ ] 2.4 Extract Big Momma mortar projectile (`bigmomma_mortar.h`, `bigmomma_mortar.cpp`)
    - Move `CBMortar` out of `bigmomma.cpp`.
    - _Requirements: REQ-4.2_
  - [ ] 2.5 Extract Controller energy attacks (`controller_energy.h`, `controller_energy.cpp`)
    - Move `CControllerHeadBall` and `CControllerZapBall` out of `controller.cpp`.
    - _Requirements: REQ-4.2_
  - [ ] 2.6 Extract Gargantua flame & stomp effects (`gargantua_effects.h`, `gargantua_effects.cpp`)
    - Move `CSpiral`, `CStomp`, and `CSmoker` out of `gargantua.cpp`.
    - _Requirements: REQ-4.2_
  - [ ] 2.7 Extract Nihilanth energy attacks (`nihilanth_energy.h`, `nihilanth_energy.cpp`)
    - Move `CNihilanthHVR` out of `nihilanth.cpp`.
    - _Requirements: REQ-4.2_
  - [ ] 2.8 Extract Tentacle maw entity (`tentacle_maw.h`, `tentacle_maw.cpp`)
    - Move `CTentacleMaw` out of `tentacle.cpp`.
    - _Requirements: REQ-4.2_

- [ ] 3. Navigation Node Graph (`nodes.cpp`) Modularization
  - [ ] 3.1 Extract `dlls/ai/nodes_graph.cpp`
    - Implement graph initialization, memory allocation, node hashing, `CNodeEnt`, and disk serialization.
    - _Requirements: REQ-4.3_
  - [ ] 3.2 Extract `dlls/ai/nodes_routing.cpp`
    - Implement shortest path routing, Floyd-Warshall route search, `PathLength`, `NextNodeInRoute`, `CStack`, and `CQueue`.
    - _Requirements: REQ-4.3_
  - [ ] 3.3 Extract `dlls/ai/nodes_links.cpp`
    - Implement node link testing, hull trace drop tests (`CTestHull`), and dynamic link lookups.
    - _Requirements: REQ-4.3_
  - [ ] 3.4 Extract `dlls/ai/nodes_debug.cpp`
    - Implement node link drawing, `CheckNode`, and `CNodeViewer` visualization.
    - _Requirements: REQ-4.3_
  - [ ] 3.5 Refactor `dlls/ai/nodes.h`
    - Standardize interface declarations for graph operations, link queries, and routing methods.
    - _Requirements: REQ-4.3_

- [ ] 4. Build Systems Synchronization & CI Verification
  - [ ] 4.1 Update Visual Studio project files (`projects/vs2019/hldll.vcxproj`, `projects/vs2019/hldll.vcxproj.filters`)
    - _Requirements: REQ-4.4_
  - [ ] 4.2 Update Linux makefiles (`dlls/Makefile`, `linux/Makefile.hldll`)
    - _Requirements: REQ-4.4_
  - [ ] 4.3 Verify compilation and linking across MSVC and GCC
    - Run local MSBuild and ensure zero warnings or errors.
    - _Requirements: REQ-4.4_
